class Solution {
    public static long seriesSum(int n) {
        // code here
        
         long sum = n;
         if(n>0)
        return sum*(sum+1)/2;
    
        else
            return 0;
    }
}
